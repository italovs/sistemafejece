
<div class="col-lg-3 col-md-5 auth-form mx-auto mt-3">
  <div class="card">
    <div class="card-body">
      <img class="auth-form__logo d-table mx-auto mb-3" src="/images/fejece_logo_fundo_branco.jpg" alt="Logo FEJECE">
      <h5 class="auth-form__title text-center mb-4">Crie sua conta</h5>
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>

        <div class="form-group">
          <label for="exampleInputEmail1">Nome completo</label>
          <%= f.text_field :name, autofocus: true, autocomplete: "name", class:"form-control" %>
        </div>

        <div class="form-group">
          <label for="exampleInputEmail1">Email</label>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"form-control" %>
        </div>

        <div class="form-group">
          <label>Escolha sua empresa júnior</label>
          <%= collection_select(:member,:junior_enterprise_id, JuniorEnterprise.all.order(:name) ,:id,:name, {},{:class =>'form-control custom-select custom-select-sm'})%>
        </div>
        <div class="form-group">
          <label>Qual o seu cargo?</label>
          <%= f.select :position, Member.positions,{}, {:class=>'form-control custom-select custom-select-sm'}%>
        </div>
        <div class="form-group">
          <label>Senha</label>
          <% if @minimum_password_length %>
          <em>(<%= @minimum_password_length %> caracteres pelo menos)</em>
          <% end %><br />
          <%= f.password_field :password, autocomplete: "current-password", class: "form-control" %>
        </div>

        <div class="form-group">
          <label>Confirme sua senha</label>
          <%= f.password_field :password_confirmation, autocomplete: "Confirme a senha",class:"form-control" %>
        </div>

        <div class="actions">
          <%= f.submit "Cadastrar", class:"mb-2 btn btn-sm btn-pill btn-outline-primary mr-2" %>
        </div>
        <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
<% end %>

      
